from django.test import TestCase

# Create your tests here.
# Form testing does not really work, as we have no Django forms but only self-designed HTML forms.
# Those we can hopefully test completely in the test_views
# There is no specific API support for testing in Django that your HTML output is rendered as expected
