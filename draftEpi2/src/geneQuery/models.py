from django.db import models

# Create your models here.

# We write the Forms for the Query input separately, here we write the models representing
# our DB, in which the Query setting are applied on

# class geneLocation(models.Model):
#         # geneID = models.CharField(max_length=11, null=False)
#         chromosome = models.IntegerField(null=False)
#         # gene_start = models.IntegerField(null=False)
#         # gene_end = models.IntegerField(null=False)
#
#
# class REM(models.Model):
#         # REMID = models.CharField(max_length=50)
#         chromosome = models.IntegerField(null=False)
#         # REM_start = models.IntegerField(null=False)
#         # REM_end = models.IntegerField(null=False)
#         # coefficient = models.FloatField(null=True)
#
# # class genomeAnnotation(models.Model):
# #         genomeVersion = models.IntegerField(null=False)

